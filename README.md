# Degree Completion Visualizer 
## Usage
Take note that files are populated from a local directory in CSVReader.java line 258. This must be changed to fit the directory of the user for the program to function.
